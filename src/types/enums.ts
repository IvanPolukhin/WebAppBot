export enum RoutesPath {
  COUNTER = '/',
  REGISTER = '/register',
  SETTING = '/setting',
}

export enum Language {
  EN = 'en',
  UA = 'uk',
}

export enum ThemeVariants {
  LIGHT = 'light',
  DARK = 'dark',
}

export enum LocalStorageKeys {
  LOCALE = 'locale',
  THEME = 'theme',
}
